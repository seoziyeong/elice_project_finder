import { colors } from "@/styles/colors";
import styled from "@emotion/styled";

export const Title = styled.h1`
  width: 1200px;
  margin: 0 auto;
  font-size: 32px;
  font-weight: 700;
  line-height: 38px;
  color: ${colors.gray1};
`;
